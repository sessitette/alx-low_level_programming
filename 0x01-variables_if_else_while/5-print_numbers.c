#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Function prints single digit base 10 numbers from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");

	return (0);
}
